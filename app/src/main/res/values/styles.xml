<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_500</item>
        <item name="colorPrimaryVariant">@color/dark_200</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:colorBackground">@color/dark_700</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/purple_200</item>
        <item name="colorSecondaryVariant">@color/purple_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <item name="android:textColor">#ffffffff</item>
    </style>

    <style name="ActionBar.nameText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextStyle" parent="android:Widget.TextView">
        <item name="fontFamily">@font/sourcesanspro_regular</item>
    </style>

    <style name="ThemeEditTextLight">
        <!-- bottom line color -->
        <item name="colorControlNormal">@color/white</item>
        <!-- text color -->
        <item name="android:textColor">@color/white</item>
        <!-- hint text color -->
        <item name="android:textColorHint">@color/white</item>
    </style>

    <style name="ButtonBorderLightWithRadius">
        <item name="paddingStart">4dp</item>
        <item name="paddingEnd">4dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:foreground">?attr/selectableItemBackground</item>
        <item name="android:background">@drawable/btn_border_light_with_radius</item>
        <item name="android:textColor">@color/purple_500</item>
        <item name="textAllCaps">false</item>
        <item name="android:layout_height">@dimen/button_height</item>
    </style>

    <style name="ButtonBorderLight">
        <item name="paddingStart">4dp</item>
        <item name="paddingEnd">4dp</item>
        <item name="android:textSize">17sp</item>
        <item name="android:foreground">?attr/selectableItemBackground</item>
        <item name="android:background">@drawable/btn_border_light</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_height">@dimen/button_height</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="BottomSheetDialogFloatingRounded" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowSoftInputMode">adjustResize|stateVisible</item>
        <item name="bottomSheetStyle">@style/BottomSheetTransparent</item>
    </style>

    <style name="BottomSheetTransparent" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>


</resources>
